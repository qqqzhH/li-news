# KNOWN-ISSUES — 已知问题台账

> 规则：新发现问题先登记这里再决定是否处理；解决后移入底部"已解决"留痕。
> 状态取值：`开放` / `豁免`（有理由不修）/ `观察中` / `待用户`。

## 开放

### #1 scrapeNewsSource 时间与链接失真
- **描述**：新浪/雷锋网抓取的条目 `publishedAt` 一律为抓取时刻、`sourceUrl` 是列表页而非文章页（fetch-news.js scrapeNewsSource）。
- **严重程度**：中
- **影响**：这类条目永远排当天最前、72h 新鲜度闸门对它们失效（只能等 3 个月窗口淘汰）；"原文↗"跳到栏目页。
- **临时方案**：无（scrape 实测产量极低，2026-09-26 实测 Exa 置空时 scrape 贡献≈0，危害被天然抑制）。
- **下一步**：若保留 scrape 源，接入页面日期解析；若确认无价值，整个移除 scrape 更精简。

### #2 pre-push 钩子不在版本控制内
- **描述**：单一写入者的第二道保险（.git/hooks/pre-push）是本地文件，重 clone 即丢失。
- **严重程度**：低（本机是唯一开发端）
- **影响**：新环境误推数据文件不会被拦截。
- **临时方案**：README「单一写入者政策」有文字说明；tag 可回档。
- **下一步**：钩子入库（scripts/hooks/ + 一行安装说明）。

### #3 采集产出阈值 20 未经长期验证
- **描述**：静默降级告警阈值由单日双场景实测标定（正常 34 / Exa 挂 15），周末/节假日分布未知。
- **严重程度**：低
- **影响**：可能误报（收到不必要的 WARN 邮件）或漏报。
- **临时方案**：误报无害（一封邮件），阈值在 daily-collection.sh 一处可调。
- **下一步**：观察 2-4 周邮件，误报则调低、漏报则调高。

### #4 分类页 HTML 内嵌全量数据（~3.5MB）
- **描述**：分类页是构建期渲染，全量数据进 HTML/RSC payload；feed 上限和 PagedNewsList 只解决了条数与 DOM，没解决 HTML 体积。
- **严重程度**：中（移动端体验）
- **影响**：ai.html 构建产物 ~3.5MB 且随数据量缓涨（92 天窗口封顶约 2000 条）。
- **临时方案**：无（架构级，动它改变 D1 的"构建期固化"行为）。
- **下一步**：见 ARCHITECTURE「未来重构方向」#1，单独立项。

### #5 /opt/GBrain 有不明写入者反复修改 antigravity 档案
- **描述**：服务器 GBrain 数据克隆的 `2026-09-25-antigravity-proxy-hangzhou.md` 一天内三次被本地改动挡住 git pull（非本项目所为，疑似服务器上另一个 AI 会话）。
- **严重程度**：低（与本项目无关，但污染同步流程）
- **影响**：服务器 GBrain 同步需要人工 `reset --hard origin/main`。
- **临时方案**：已用 reset 对齐（杂散改动留 stash@{0} 可查）。
- **下一步**：查明哪个会话在写；注意 `/opt/GBrain` 分支名是 **main**，reset 用 origin/master 会静默无效。

### #6 墨极·20260828 全录快照全网缺失
- **描述**：8 月往期快照只有 8/21-8/27 共 7 份（li-news public/ 内），8/28 的那份历史上从未进入任何仓库。
- **严重程度**：低（历史数据缺口，非回归）
- **影响**：往期体系有 8/28 一天空白。
- **临时方案**：无。
- **下一步**：除非用户找到本地旧拷贝，否则接受缺口（不要用今日数据补造）。

## 豁免

### #7 npm audit 14 漏洞（sharp/libvips 链）
- **状态**：豁免（ARCHITECTURE D8 已验证零暴露面）。**勿跑 `npm audit fix --force`**。

### #8 Sidebar"推荐"导航跨页行为
- **描述**：非首页点"推荐"（第二屏锚点）只会跳回首页顶部，不滚到第二屏（Sidebar.tsx scrollTo）。
- **状态**：豁免（UX 边角，改动涉及跨页滚动状态传递，价值低于成本）。

## 待用户

### #9 Exa 旧 key（尾号 f47f）作废
- **描述**：新 key 已生效，旧 key 需要用户登录 Exa 后台手动作废（我们无账号权限）。
- **状态**：等待用户。作废后次日看采集邮件/日志确认无异常即可。

## 已解决（留痕）

| 日期 | 问题 | 处置 |
|---|---|---|
| 2026-09-26 | feed.xml 无上限（858KB+ 持续膨胀） | 限最近 100 条（66KB），feed.test 断言锁定 |
| 2026-09-26 | 分类页全量 DOM 渲染（577 卡） | PagedNewsList 首屏 50 + 加载更多 |
| 2026-09-26 | other 分类空页误导文案 + 死入口 | 入口隐藏（CATEGORIES.hidden）+ 文案中性化 |
| 2026-09-26 | Sidebar RSS 死文本 | 改为 /feed.xml 真链接 |
| 2026-09-26 | moji_daily/archive 冻结旧副本 | 删除 + _redirects 302 主站（防回归测试锁定） |
| 2026-09-26 | 双写入端 git 分叉（17 提交） | union 合并 + 单一写入者政策 + pre-push 钩子 |
| 2026-09-26 | GenMojiDaily 连败 19 天 | 脏工作区清理 + 脚本自愈加固 |
| 2026-09-26 | 采集静默降级无告警 | NEW_ITEMS_COUNT + 阈值 20 WARN 邮件 |
| 2026-09-26 | cron 并发竞态 / 日志无轮转 / feed 域名占位符 / EXA key 入库 / npm test 缺失 | 各自小修（见 CHANGELOG） |
